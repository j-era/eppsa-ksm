{
  "name": "hidden",
  "version": "0.1.0",
  "scripts": {
    "watch": "http-server src/ -p $PORT",
    "start": "http-server src/ -p $PORT -c 0"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
