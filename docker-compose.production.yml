version: "3.2"

services:
  nginx:
    ports:
      - "80:80"
      - "443:443"

  frontend:
    build:
      dockerfile: Dockerfile.production
    environment:
      - PORT=80

  game-client:
    build:
      dockerfile: Dockerfile.production

  game-server:
    build:
      dockerfile: Dockerfile.production
